Review the current conversation and extract any potential issues, known limitations, deferred improvements, or technical debt that was identified but not fixed. Append them to `Hoang-dev-log/future-fix.md`.

For each issue found:
1. Add a `## [YYYY-MM-DD] <short title>` header using today's date
2. Fill in: **File** (with line number if known), **Risk** (Low/Medium/High), **Impact** (what breaks if ignored), **Fix** (exact code or SQL to resolve it)
3. Do not duplicate entries that already exist in the file — check existing content first

Only include issues that are real and actionable. Do not add speculative or already-fixed items.
